# --- web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0

# --- database ---
sqlalchemy>=2.0.0
alembic>=1.13.0
greenlet>=3.0.0          # required by SQLAlchemy's async bridge
aiosqlite>=0.19.0        # async driver for local SQLite
asyncpg>=0.29.0          # async driver for Postgres (production)
psycopg2-binary>=2.9.9   # sync driver, used by Alembic against Postgres

# --- auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyotp>=2.9.0
qrcode>=8.0
openpyxl>=3.1.0        # .xlsx export of financial records

# --- email ---
# Used by the Resend path in app/services/mailer.py, and by every test that
# drives the app through AsyncClient. It was missing from this file while
# being present in the development virtualenv, so the whole suite passed and
# the deploy still died on `import httpx` before uvicorn could bind a port.
httpx>=0.27.0

# --- assistant ---
google-generativeai>=0.4.0
